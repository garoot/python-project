# Tip Calculator

A CLI Python program that calculates the total bill after adding a tip percentage.

## Run

```
python3 main.py
```

Enter the bill amount and tip percentage when prompted. The program will print the final total.

## Test

```
python3 -m pytest
```

The included test checks that a bill of `100` with a `10%` tip returns `110`.
